{{- if .Values.ingress.enabled }}
LinkShelf: {{ include "linkshelf.frontendUrl" . }}
API:       {{ include "linkshelf.apiUrl" . }}
{{- else }}
No ingress configured. To try it locally:

  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "linkshelf.fullname" . }} 3000:{{ .Values.service.frontendPort }} 8085:{{ .Values.service.backendPort }}

and open http://localhost:3000.
{{- end }}
